(version 5.4.1)

(depends
 (choice
  ((((arch x86_64)
     (os macos))
    ((arch arm64)
     (os macos)))
   (ocaml-compiler))))

(enabled_on_platforms
 (only
  ((arch x86_64)
   (os macos))
  ((arch arm64)
   (os macos))))
