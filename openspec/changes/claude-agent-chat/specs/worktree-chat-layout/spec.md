## REMOVED Requirements

### Requirement: Keep worktree command controls visible
**Reason**: The worktree response screen and its shortcut controls are retired.
**Migration**: `claude-agent-chat` requires a scrolling conversation with a visible composer; OpenCode keeps its existing layout.
